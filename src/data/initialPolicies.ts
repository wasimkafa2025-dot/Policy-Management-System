import { Policy, SystemNotification, AuditLog } from "../types";

export const initialPolicies: Policy[] = [];

export const initialNotifications: SystemNotification[] = [];

export const initialAuditLogs: AuditLog[] = [];
